# web应用程序
# import socket
# socket = socket.socket()  # 构建socket类
# socket.bind(("127.0.0.1", 8090))
# socket.listen(5)
# while 1:
#     conn, addr = socket.accept() #  阻塞等待客户
#     data = conn.recv(1024)
#     print("require:\n", data)
#     req = conn.send(b"HTTP/1.1 200 ok\r\nserver:aaron\r\n\r\nhello world")
#     conn.close()

